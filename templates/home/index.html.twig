{% extends 'base.html.twig' %}

{% block title %}Sopranos | Home{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: auto; text-align: center; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Categorieën ({{ categories|length }})</h1>
</div>

    {% for category in categories %}

        <div class="card" style="width: 30rem;">
            <img class="card-img-top" src="..." alt="Card image cap">
            <div class="card-body">
                <h5 class="card-title">{{ category.name }}</h5>
                <p class="card-text">Bekijk de lekkerste pizza's bij deze categorie.</p>
                <a href="{{ path('app_cat', {id:category.id}) }}" class="btn btn-danger">Naar Pizza: {{ category.name }}</a>
            </div>
        </div>

    {% endfor %}



{% endblock %}
